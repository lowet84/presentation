version: '3'
services:
  presentation:
    build: .
    networks: 
      - kubeadm-dind-net
    privileged: true
    ports:
      - 3000:3000
    hostname: server

networks:
  kubeadm-dind-net:
    external: true
